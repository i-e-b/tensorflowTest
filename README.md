# Basics and experiments in using TensorFlow.

## Setup
Install python 3.5+
`pip3 install --upgrade tensorflow`
`pip3 install --upgrade numpy`
`pip install Pillow`

## Running
`python `filename
